// Styling TextInput

// Example:
const styles = StyleSheet.create({
  input: {
    height: 50,
    borderColor: "#3F51B5",
    borderWidth: 2,
    borderRadius: 5,
    paddingHorizontal: 15,
    fontSize: 16,
    backgroundColor: "#F5F5F5",
  },
});

// Result:
// A styled input field with increased height, rounded borders, padding, and a subtle background color.
